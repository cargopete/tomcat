# Generated by yatr rollout — adjust as needed.

[tasks.fmt]
desc = "Format (ruff)"
run = ["ruff format ."]

[tasks.lint]
desc = "Lint (ruff)"
run = ["ruff check ."]

[tasks.test]
desc = "Run tests"
run = ["pytest"]

[tasks.ci]
desc = "CI pipeline"
depends = ["lint", "test"]
